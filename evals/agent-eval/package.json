{
  "name": "cue-agent-eval",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "A/B test cue PROFILES with @vercel/agent-eval — same task, same model, the profile's CLAUDE.md is the only variable.",
  "scripts": {
    "dry": "agent-eval --dry",
    "smoke": "agent-eval --smoke",
    "eval": "agent-eval",
    "skill:dry": "bun skill-ab/local.ts --dry",
    "skill:smoke": "bun skill-ab/local.ts --smoke",
    "skill:eval": "bun skill-ab/local.ts --full",
    "skill:report": "bun skill-ab/report.ts",
    "skill:test": "bun test skill-ab/contract.test.ts skill-ab/report.test.ts skill-ab/local.test.ts",
    "collaboration:test": "bun test collaboration-ab/contract.test.ts",
    "collaboration:dry": "bun collaboration-ab/cli.ts --dry",
    "collaboration:report": "bun collaboration-ab/cli.ts --report",
    "collaboration:typecheck": "tsc -p collaboration-ab/tsconfig.json",
    "collaboration:lint": "biome lint collaboration-ab"
  },
  "devDependencies": {
    "@vercel/agent-eval": "1.2.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
